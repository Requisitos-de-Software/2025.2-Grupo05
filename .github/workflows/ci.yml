# .github/workflows/ci.yml

name: CI/CD Pipeline

on:
  # Executar em push para main e devel
  push:
    branches:
      - main
      - devel

  # Executar em pull requests para main e devel (PROTEÇÃO)
  pull_request:
    branches:
      - main
      - devel
    types: [opened, synchronize, reopened]

# Configurações globais
env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "18"

jobs:
  # ================================================================
  # JOB 1: VALIDAÇÃO COMPLETA (Required para PRs)
  # ================================================================
  validate:
    name: "🔍 Validate Code & Docs"
    runs-on: ubuntu-latest

    steps:
      # Checkout com fetch completo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Setup Python otimizado
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      # Instalar dependências
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
          pip install ruff black bandit safety

      # Validar estrutura de arquivos
      - name: Check required files
        run: |
          test -f requirements.txt || (echo "❌ requirements.txt not found" && exit 1)
          test -f mkdocs.yml || (echo "❌ mkdocs.yml not found" && exit 1)
          echo "✅ Required files present"

      # Segurança - verificar vulnerabilidades
      - name: Security check with Bandit
        run: |
          bandit -r . -f json -o bandit-report.json || true
          bandit -r . --severity-level medium || echo "⚠️ Security warnings found"

      # Segurança - verificar dependências
      - name: Check dependencies for vulnerabilities
        run: |
          safety check --json --output safety-report.json || echo "⚠️ Dependency vulnerabilities found"

      # Lint Python com Ruff
      - name: Lint Python with Ruff
        run: |
          echo "🔍 Running Ruff linting..."
          ruff check . --output-format=github

      # Verificar formatação Python
      - name: Check Python formatting with Black
        run: |
          echo "🎨 Checking Python code formatting..."
          black --check --diff --color .

      # Validar MkDocs build
      - name: Validate MkDocs build
        run: |
          echo "📚 Building MkDocs documentation..."
          mkdocs build --strict --verbose

      # Setup Node.js para markdownlint
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      # Validar Markdown
      - name: Lint Markdown files
        uses: DavidAnson/markdownlint-cli2-action@v15
        with:
          globs: |
            **/*.md
            !site/**
            !node_modules/**
            !venv/**
          config: |
            {
              "MD013": { "line_length": 120 },
              "MD033": false,
              "MD041": false
            }

      # Upload artifacts em caso de falha
      - name: Upload validation artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: validation-reports
          path: |
            bandit-report.json
            safety-report.json
          retention-days: 7

  # ================================================================
  # JOB 2: TESTES (Se houver)
  # ================================================================
  test:
    name: "🧪 Run Tests"
    runs-on: ubuntu-latest
    needs: validate
    if: success()

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run tests (if present)
        run: |
          if [ -d "tests" ] || [ -f "test_*.py" ]; then
            echo "🧪 Running tests..."
            pytest --cov=. --cov-report=xml --cov-report=html
          else
            echo "ℹ️ No tests found, skipping..."
          fi

      - name: Upload coverage reports
        if: success() && hashFiles('coverage.xml') != ''
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          flags: unittests

  # ================================================================
  # JOB 3: SUGESTÕES DE CORREÇÃO (apenas em falha)
  # ================================================================
  fix-suggestions:
    name: "🛠️ Fix Suggestions"
    runs-on: ubuntu-latest
    if: failure() && (needs.validate.result == 'failure' || needs.test.result == 'failure')
    needs: [validate, test]

    steps:
      - name: Show fix commands
        run: |
          echo "################################################################"
          echo "#                                                              #"
          echo "#          ❌ ERROS ENCONTRADOS - COMANDOS PARA CORRIGIR       #"
          echo "#                                                              #"
          echo "################################################################"
          echo ""
          echo "🎨 Para corrigir formatação Python:"
          echo "   black ."
          echo ""
          echo "🔧 Para corrigir erros de lint Python:"
          echo "   ruff check . --fix"
          echo ""
          echo "📝 Para corrigir erros de Markdown:"
          echo "   npx markdownlint-cli2 --fix \"**/*.md\" \"#site\""
          echo ""
          echo "🔒 Para verificar segurança:"
          echo "   bandit -r ."
          echo "   safety check"
          echo ""
          echo "📚 Para testar MkDocs:"
          echo "   mkdocs build --strict"
          echo ""
          echo "🧪 Para rodar testes:"
          echo "   pytest"
          echo ""
          echo "################################################################"

  # ================================================================
  # JOB 4: DEPLOY (apenas main com validação aprovada)
  # ================================================================
  deploy:
    name: "🚀 Deploy Documentation"
    runs-on: ubuntu-latest
    needs: [validate, test]
    # Deploy apenas em push para main (não PRs) e com validação OK
    if: |
      success() && 
      github.event_name == 'push' && 
      github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: Install documentation dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build documentation
        run: |
          echo "📚 Building documentation for deployment..."
          mkdocs build --verbose

      - name: Deploy to GitHub Pages
        id: deployment
        run: |
          echo "🚀 Deploying to GitHub Pages..."
          mkdocs gh-deploy --force --clean --verbose
          echo "page_url=https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ================================================================
  # JOB 5: NOTIFICAÇÃO DE STATUS
  # ================================================================
  notify:
    name: "📢 Notify Status"
    runs-on: ubuntu-latest
    needs: [validate, test, deploy]
    if: always()

    steps:
      - name: Notify success
        if: needs.validate.result == 'success' && needs.test.result == 'success'
        run: |
          echo "✅ Pipeline executada com sucesso!"
          echo "📚 Documentação: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"

      - name: Notify failure
        if: needs.validate.result == 'failure' || needs.test.result == 'failure'
        run: |
          echo "❌ Pipeline falhou. Verifique os logs acima para detalhes."
          echo "💡 Execute os comandos de correção sugeridos."
