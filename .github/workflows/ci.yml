# .github/workflows/ci.yml

name: CI/CD Pipeline

on:
  # Executar em push para main e devel
  push:
    branches:
      - main
      - devel

  # Executar em pull requests para main e devel (PROTEÇÃO)
  pull_request:
    branches:
      - main
      - devel
    types: [opened, synchronize, reopened]

# Configurações globais
env:
  PYTHON_VERSION: "3.11"

jobs:
  # ================================================================
  # JOB 1: VALIDAÇÃO DE DOCUMENTAÇÃO (Required para PRs)
  # ================================================================
  validate:
    name: "🔍 Validate Documentation"
    runs-on: ubuntu-latest

    steps:
      # Checkout com fetch completo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Setup Python otimizado
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      # Instalar dependências do MkDocs
      - name: Install MkDocs dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Validar estrutura de arquivos obrigatórios
      - name: Check required files
        run: |
          test -f requirements.txt || (echo "❌ requirements.txt not found" && exit 1)
          test -f mkdocs.yml || (echo "❌ mkdocs.yml not found" && exit 1)
          test -d docs || (echo "❌ docs/ directory not found" && exit 1)
          echo "✅ Required files and directories present"

      # Verificar sintaxe de arquivos Python (animações)
      - name: Check Python syntax
        run: |
          echo "🐍 Checking Python syntax for animation scripts..."
          find . -name "*.py" -type f -exec python -m py_compile {} \; || (echo "❌ Python syntax errors found" && exit 1)
          echo "✅ Python syntax validation passed"

      # Validar build do MkDocs (estrutura e links)
      - name: Validate MkDocs build
        run: |
          echo "📚 Building MkDocs documentation..."
          mkdocs build --strict --verbose

      # Validar Markdown com configuração específica para documentação
      - name: Lint Markdown files
        uses: DavidAnson/markdownlint-cli2-action@v15
        with:
          # Usar configuração do arquivo .markdownlint-cli2.jsonc se existir
          config: |
            {
              "MD013": { "line_length": 120, "code_blocks": false, "tables": false },
              "MD033": false,
              "MD041": false,
              "MD036": false,
              "MD001": true,
              "MD022": { "lines_above": 1, "lines_below": 1 }
            }
          globs: |
            **/*.md
            !site/**
            !node_modules/**
            !venv/**

      # Verificar estrutura básica da documentação
      - name: Check documentation structure
        run: |
          echo "📋 Checking documentation structure..."

          # Verificar se existe README principal
          test -f README.md || echo "⚠️ README.md not found in root"

          # Verificar estrutura básica do docs/
          test -f docs/index.md || echo "⚠️ docs/index.md not found"

          # Listar estrutura para debug
          echo "📁 Current documentation structure:"
          find docs -type f -name "*.md" | head -20 || echo "No markdown files found in docs/"

          echo "✅ Documentation structure check completed"

  # ================================================================
  # JOB 2: SUGESTÕES DE CORREÇÃO (apenas em falha)
  # ================================================================
  fix-suggestions:
    name: "🛠️ Fix Suggestions"
    runs-on: ubuntu-latest
    if: failure() && needs.validate.result == 'failure'
    needs: validate

    steps:
      - name: Show fix commands
        run: |
          echo "################################################################"
          echo "#                                                              #"
          echo "#          ❌ ERROS DE DOCUMENTAÇÃO - COMANDOS PARA CORRIGIR   #"
          echo "#                                                              #"
          echo "################################################################"
          echo ""
          echo "📝 Para corrigir erros de Markdown:"
          echo "   npx markdownlint-cli2 --fix \"**/*.md\" \"#site\""
          echo ""
          echo "🐍 Para verificar sintaxe Python (animações):"
          echo "   python -m py_compile arquivo.py"
          echo ""
          echo "📚 Para testar build do MkDocs:"
          echo "   mkdocs build --strict"
          echo ""
          echo "🔍 Para servir localmente:"
          echo "   mkdocs serve"
          echo ""
          echo "📋 Para verificar estrutura:"
          echo "   ls -la docs/"
          echo "   cat mkdocs.yml"
          echo ""
          echo "################################################################"

  # ================================================================
  # JOB 3: DEPLOY (apenas main com validação aprovada)
  # ================================================================
  deploy:
    name: "🚀 Deploy Documentation"
    runs-on: ubuntu-latest
    needs: validate
    # Deploy apenas em push para main (não PRs) e com validação OK
    if: |
      success() && 
      github.event_name == 'push' && 
      github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: Install documentation dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build documentation
        run: |
          echo "📚 Building documentation for deployment..."
          mkdocs build --verbose

      - name: Deploy to GitHub Pages
        id: deployment
        run: |
          echo "🚀 Deploying to GitHub Pages..."
          mkdocs gh-deploy --force --clean --verbose
          echo "page_url=https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ================================================================
  # JOB 4: NOTIFICAÇÃO DE STATUS
  # ================================================================
  notify:
    name: "📢 Notify Status"
    runs-on: ubuntu-latest
    needs: [validate, deploy]
    if: always()

    steps:
      - name: Notify success
        if: needs.validate.result == 'success'
        run: |
          echo "✅ Documentação validada com sucesso!"
          echo "📚 Site: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"

      - name: Notify failure
        if: needs.validate.result == 'failure'
        run: |
          echo "❌ Validação da documentação falhou."
          echo "💡 Verifique os erros de Markdown, Python ou estrutura acima."
