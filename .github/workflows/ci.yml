name: CI - Docs & Code

on:
  # Roda a pipeline em todo push e PR para main e devel
  push:
    branches:
      - main
      - devel
  pull_request:
    branches:
      - main
      - devel
    types: [opened, synchronize, reopened]

jobs:
  # ===================================================================
  # 1º JOB: Validação Geral de Código, Markdown e Documentação
  # ===================================================================
  ci:
    name: "🔍 CI: Validação Geral"
    runs-on: ubuntu-latest

    steps:
      # Checkout do código do repositório
      - name: 🔦 Checkout
        uses: actions/checkout@v4

      # Prepara o Python para tarefas do projeto
      - name: 🧰 Setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Instala dependências listadas no projeto e ferramentas extra de lint/docs
      - name: 💾 Instalar dependências
        run: |
          pip install -r requirements.txt
          pip install ruff mkdocs markdownlint-cli2

      # Checa se o formato do código Python segue o padrão (PEP 8 e Black/Ruff)
      - name: 🎨 Ruff format (checar estilo)
        run: |
          ruff format --check .
        continue-on-error: true # Segue mesmo se falhar para mostrar todos os erros no final

      # Faz lint completo, apontando possíveis bugs e antipadrões no Python
      - name: 🛁 Ruff check (bug/lint)
        run: |
          ruff check .
        continue-on-error: true

      # Verifica a sintaxe de todos os arquivos Python do repositório (ex.: animações)
      - name: 🐍 Python syntax (animações)
        run: |
          set +e
          find . -name "*.py" -type f -exec python -m py_compile {} \;
          result=$?
          if [ "$result" != "0" ]; then
            echo "❌ Python syntax errors found"
            exit 1
          fi

      # Faz análise de estilo dos arquivos Markdown usando markdownlint-cli2
      - name: 📝 MarkdownLint (markdownlint-cli2)
        uses: DavidAnson/markdownlint-cli2-action@v15
        with:
          globs: |
            **/*.md
            !site/**
            !node_modules/**
            !venv/**

      # Build da documentação com MkDocs (valida links, estrutura, nav etc)
      - name: 📚 MkDocs build
        run: |
          mkdocs build --strict --verbose

      # Se algum passo acima falhar, exibe dicas de comandos para corrigir rapidamente
      - name: ❌ Exibir comandos de correção se falhar
        if: failure()
        run: |
          echo "################################################################"
          echo "#         ❌ ERROS ENCONTRADOS - COMO CORRIGIR                  #"
          echo "################################################################"
          echo "🦄 Ruff format:  Corrija estilo automaticamente:"
          echo "  ruff format ."
          echo ""
          echo "⭐ Ruff check: Visualize/corrija bugs/erros:"
          echo "  ruff check . --fix"
          echo ""
          echo "🐍 Python: Corrija sintaxe dos scripts Python:"
          echo "  python -m py_compile arquivo.py"
          echo ""
          echo "📝 Markdown: Corrija formatação Markdown:"
          echo "  npx markdownlint-cli2 --fix \"**/*.md\" \"!site\""
          echo ""
          echo "📚 MkDocs: Verifique estrutura/links:"
          echo "  mkdocs build --strict"
          echo "################################################################"
          exit 1

  # ===================================================================
  # 2º JOB: Deploy da documentação (SÓ se todas as validações passarem)
  # ===================================================================
  deploy:
    name: "🚀 Deploy Docs"
    needs: ci
    runs-on: ubuntu-latest
    # Só faz deploy se todas validações passaram, push na main (não PRs)
    if: ${{ success() && github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    steps:
      # Checkout da branch main para deploy final
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Prepara Python para rodar o MkDocs
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Instala somente o necessário para gerar/docs
      - name: Instalar dependências MkDocs
        run: |
          pip install -r requirements.txt
          pip install mkdocs

      # Gera build da documentação do zero
      - name: Build docs
        run: mkdocs build --verbose

      # Faz deploy do site para o GitHub Pages (gh-pages branch)
      - name: Deploy to GitHub Pages
        run: mkdocs gh-deploy --force --clean --verbose
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

